name: Generate Game Results
on:
  schedule:
    - cron: '0 0 */2 * *' # Runs at 00:00 every 2 days
  workflow_dispatch: # Allows manual triggering

jobs:
  run-r-script:
    runs-on: ubuntu-latest
    container:
      image: rocker/tidyverse:4.1.0 # Pre-built R image with tidyverse

    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Install additional packages
        run: |
          install.packages(c("jsonlite"), repos = "https://cloud.r-project.org")
        shell: Rscript {0}

      - name: Run R script
        run: Rscript scripts/generate-game-results.r

      - name: Commit and push if changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git diff --staged --quiet || (git commit -m "Update data" && git push)
